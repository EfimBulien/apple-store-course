@model TechStoreEll.Web.Models.AdminPanelViewModel

@{
    ViewData["Title"] = "Админ-панель";
}

<p>Только для админов 🎩</p>
<div class="mb-4">
    <div class="d-flex flex-wrap gap-2">
        <a asp-controller="Warehous" asp-action="Index" class="btn btn-primary">
            Склады
        </a>
        <a asp-controller="Role" asp-action="Index" class="btn btn-primary">
            Роли
        </a>
        <a asp-controller="Category" asp-action="Index" class="btn btn-primary">
            Категории
        </a>
        <a asp-controller="Admin" asp-action="Export" class="btn btn-info">
            Экспорт данных
        </a>
        <a asp-action="ReviewModeration" class="btn btn-outline-warning">
            📝 Модерация отзывов
            @if (ViewBag.UnmoderatedCount > 0)
            {
                <span class="badge bg-danger">@ViewBag.UnmoderatedCount</span>
            }
        </a>
    </div>
    
</div>

<h2>Журнал аудита</h2>
<div class="mb-3">
    <form method="get" class="row g-2">
        <div class="col-auto">
            <p>Выберите количество последних записей</p>
            <label for="take" class="visually-hidden">Количество записей</label>
            <input type="number"
                   id="take"
                   name="take"
                   value="@Model.Take"
                   min="10"
                   max="1000"
                   class="form-control"
                   style="width: 120px;"/>
            <div class="col-auto mt-3">
                <button type="submit" class="btn btn-primary">Применить</button>
            </div>
        </div>
    </form>
</div>

<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>Дата и время</th>
                <th>Пользователь</th>
                <th>Таблица</th>
                <th>Операция</th>
                <th>ID записи</th>
                <th>Изменения</th>
            </tr>
        </thead>
        <tbody>
            @if (Model.AuditLogs.Any())
            {
                @foreach (var log in Model.AuditLogs)
                {
                    <tr>
                        <td>@log.ChangedAt.ToString("dd.MM.yyyy HH:mm:ss")</td>
                        <td>
                            @(log.ChangedByNavigation != null 
                                ? $"{log.ChangedByNavigation.FirstName} " +
                                  $"{log.ChangedByNavigation.LastName} " +
                                  $"({log.ChangedByNavigation.Email})" : "Система")
                        </td>
                        <td><code>@log.TableName</code></td>
                        <td>
                            @{
                                var opText = log.Operation switch
                                {
                                    'I' => "Создание",
                                    'U' => "Обновление",
                                    'D' => "Удаление",
                                    _ => "Неизвестно"
                                };
                                var opClass = log.Operation switch
                                {
                                    'I' => "text-success",
                                    'U' => "text-warning",
                                    'D' => "text-danger",
                                    _ => ""
                                };
                            }
                            <span class="@opClass">@opText</span>
                        </td>
                        <td>@(log.RecordId ?? "—")</td>
                        <td>
                            @if (log.OldRow != null || log.NewRow != null)
                            {
                                <details>
                                    <summary>Показать JSON</summary>
                                    @if (log.OldRow != null)
                                    {
                                        <div>
                                            <strong>Было:</strong>
                                            <pre style="font-size:0.85em;">@log.OldRow</pre>
                                        </div>
                                    }
                                    @if (log.NewRow != null)
                                    {
                                        <div>
                                            <strong>Стало:</strong> <pre style="font-size:0.85em;">@log.NewRow</pre>
                                        </div>
                                    }
                                </details>
                            }
                            else
                            {
                                <em>Нет данных</em>
                            }
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="6" class="text-center">Нет записей в журнале</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <style>
        pre {
            white-space: pre-wrap;
            word-break: break-word;
            padding: 8px;
            border-radius: 4px;
            margin: 4px 0;
            font-size: 0.85em;
        }
    </style>
}